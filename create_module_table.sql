CREATE TABLE modules
	(
		id int (11),
		name varchar (100),
		startDate long,
		endDate long,
		campus varchar (100),
		room varchar (100),
		programme varchar (100),
		level varchar (100),
		PRIMARY KEY (id)
	);